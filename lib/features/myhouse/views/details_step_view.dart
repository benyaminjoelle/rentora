import 'package:flutter/material.dart';

class FirstAddHouse extends StatelessWidget {
  const FirstAddHouse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Column(
          children: [

          ],)
          ,),
          );
  }
}